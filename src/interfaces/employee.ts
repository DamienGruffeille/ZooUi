export default interface Employee {
    _id: string;
    name: string;
    firstName: string;
    email: string;
    password: string;
    role: string;
    zone: string;
    createdAt: Date;
    updatedAt: Date;
}
